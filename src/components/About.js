import { Link } from 'react-router-dom';

const About = () => {
    return (
        <div>
            <h4>Hi :)</h4>
            <Link to='/'>Go back</Link>
        </div>
    );
};

export default About;
